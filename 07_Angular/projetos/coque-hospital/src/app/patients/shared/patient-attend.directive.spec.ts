import { PatientAttendDirective } from './patient-attend.directive';

describe('PatientAttendDirective', () => {
  it('should create an instance', () => {
    const directive = new PatientAttendDirective();
    expect(directive).toBeTruthy();
  });
});
