import { TaskConcludedDirective } from './task-concluded.directive';

describe('TaskConcludedDirective', () => {
  it('should create an instance', () => {
    const directive = new TaskConcludedDirective();
    expect(directive).toBeTruthy();
  });
});
