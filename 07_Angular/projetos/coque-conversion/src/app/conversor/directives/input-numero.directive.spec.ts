import { InputNumeroDirective } from './input-numero.directive';

describe('InputNumeroDirective', () => {
  it('should create an instance', () => {
    const directive = new InputNumeroDirective();
    expect(directive).toBeTruthy();
  });
});
