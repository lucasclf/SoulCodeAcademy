var numero = 0

numero = parseInt(prompt("Digite um numero: "))

if (numero > 0) {
    document.write("Este numero é positivo.")
} if (numero < 0) {
    document.write("Este numero é negativo.")
} if (numero == 0) {
    document.write("Este numero não é negativo nem positivo, é zero.")
}

